default: &default
  adapter: postgresql
  host: localhost
  encoding: unicode
  pool: 5

development:
  <<: *default
  database: lift_ads_development
  username:
  password:

test:
  <<: *default
  database: lift_ads_test
  username:
  password:
